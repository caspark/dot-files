# Defined in /tmp/fish.9NvW1g/post-cwd.fish @ line 2
function post-cwd
#    if test -f $PWD"/.ruby-version"
#        echo "post-cwd fish function found a .ruby-version file so doing 'rvm use'..."
#        rvm use
#    end
end
